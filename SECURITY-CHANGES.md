# 本次安全与可靠性修复

- 安装改为带 CSRF 校验的账号创建表单，全部数据写入成功后才创建配置文件作为安装标记。
- 数据操作使用 data/.storage.lock 跨进程独占锁，覆盖整个请求，避免读取旧数据后覆盖其他请求的修改。数据先写入同目录临时文件，再替换目标；失败返回 503，停止成功提示。
- 登录按来源 IP 限制：15 分钟内连续失败 5 次后暂时拒绝。Session 绑定密码版本，修改密码后撤销其他会话。升级前的会话需要重新登录。
- 编辑文章保留置顶和插件扩展字段。
- 待审核评论不发送访客提醒，首次审核通过后发送；审核后的评论数在保存后重算。
- 评论限制字段长度和主页 URL 协议，回复只能指向同文章已审核评论。历史危险主页链接不再输出。
- 更新和 SMTP 开启 TLS 证书验证。更新地址仅允许 HTTPS；cURL 重定向仅允许 HTTPS，无 cURL 时不自动跟随重定向。
- 更新检查解压结果、路径、体积、文件数量和核心文件；覆盖前备份，复制失败时回滚。回滚失败会保留备份并显示位置。
- 自动刷新在请求失败或页面隐藏后释放忙碌状态，可以继续轮询。

## 验证

运行 `php tests/regression.php`。测试使用系统临时目录中的独立数据，验证 12 个独立进程并发写入、ID 唯一性、会话撤销、危险主页过滤和原子文件替换。

## 部署注意

服务器需配置有效 CA 证书库。证书错误应修复服务器配置，不应关闭校验。Nginx 仍须禁止访问 /data/。

完整请求锁是适用于小型纯文件博客的正确性方案，慢邮件或在线更新会阻塞其他请求；后续可将邮件队列化并缩小事务边界。更新回滚处理可捕获的复制失败，不保证断电或进程被强制终止时自动恢复。更新包签名和持久化恢复日志尚未实现。

本次没有增加产品功能清单中的自动保存、历史版本、SQLite 迁移或完整备份管理页面。
